/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function (arr, fn) {

    // apply map funciton on array where fn funciton is applied to each element before resultant array is returned
    return arr.map(fn);

};